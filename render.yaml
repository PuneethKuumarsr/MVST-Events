services:
  - type: web
    name: mvstevents
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_DEVELOPER_MODE
        value: false
      - key: MANGALYA_SPONSORSHIP_SHEET_ID
        sync: false
      - key: MANGALYA_SPONSORSHIP_RANGE
        value: "'Sponsorship 2026'!A:J"
